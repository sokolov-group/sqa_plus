V1 =- einsum('ix->IX', h_ca_so, optimize = einsum_type).copy()
V1 += einsum('ix,xy->IX', h_ca_so, rdm_ca_so, optimize = einsum_type)
V1 -= einsum('ixyz,zx->IX', v_caaa_so, rdm_ca_so, optimize = einsum_type)
V1 -= 1/2 * einsum('ixyz,yzwx->IX', v_caaa_so, rdm_ccaa_so, optimize = einsum_type)
